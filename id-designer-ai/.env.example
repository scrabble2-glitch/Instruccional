# Base
NODE_ENV=development
DATABASE_URL="file:./dev.db"
APP_URL="http://localhost:3000"
# En Vercel para pruebas rápidas con SQLite efímero:
# DATABASE_URL="file:/tmp/dev.db"

# Autenticación (modo usuario único)
SINGLE_USER_PASSWORD="cambia-esta-clave"
SESSION_SECRET="cambia-este-secreto"

# Gemini
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"

# Seguridad y generación
DEFAULT_SAFETY_MODE="normal"
CACHE_TTL_MINUTES="1440"
RATE_LIMIT_PER_MINUTE="20"
INPUT_TOKEN_COST_PER_MILLION="0.10"
OUTPUT_TOKEN_COST_PER_MILLION="0.40"

# Cloudflare R2 (opcional) - para organizar por carpetas/prefijos por curso
# Crea un bucket en R2 y genera Access Key ID / Secret.
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET=""
# Prefijo base dentro del bucket donde se crearán carpetas por curso:
R2_PREFIX_BASE="cursos"
# Para R2 se recomienda "auto"
R2_REGION="auto"

# Sistema de carpetas local (opcional) - crea una carpeta por curso en tu disco.
# Ejemplo:
# LOCAL_COURSE_ROOT_DIR="/Users/tuusuario/Documents/Cursos_Story"
LOCAL_COURSE_ROOT_DIR=""
